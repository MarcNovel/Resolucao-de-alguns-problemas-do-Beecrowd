# problema 1009 - Salário com bônus

nome = input()
salario = float(input())
vendas = float(input())

total = salario + 0.15 * vendas

print('TOTAL = R$', format(total, "<.2f"))

# fim de linha